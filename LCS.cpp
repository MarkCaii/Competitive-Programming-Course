#include<bits/stdc++.h>
using namespace std;
int m, n;
dp[m][n]
int LCSW(int x, int y, string s, string t){
	if(x == m){
		if()
	}
	if(s[x] == t[y]){
		dp[x][y] = 1 + LCSW(x+1, y+1, s, t);
	}else if(){

	}
}
int main(){
	int m, n, i, j;
	string s, t;
	cin >> m >> n;
	cin >> s cin >> t;	
	cout << LCSW(m, n, s, t) << endl;	
	return 0;
}
